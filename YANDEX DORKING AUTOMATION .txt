Hidden Gems Inside Yandex Dork
by Walid Ladeb (@ladebw) — May 9, 2026 · 3,274 Views

---

The Recon Loop Most People Ignore
Most people think Yandex dorking is just another "Google dork" alternative. But for recon, Yandex can sometimes reveal a different layer of indexed assets: old routes, forgotten API paths, JavaScript files, docs, staging references, GraphQL endpoints, exposed static assets, and pages that other engines may not surface the same way.

The real value is not in one query. The real value is in the loop:

Recon → Filter → Validate → Repeat

That loop is where hidden gems appear. You are not looking for "one magic dork." You are building signal.

A good Yandex dork loop should:

Search scoped assets only
Collect URLs
Remove duplicates
Classify results
Separate APIs, JS files, docs, auth pages, staging/dev paths
Feed the clean output into your recon pipeline
Validate manually before calling anything a finding
Example dorks:

site:example.com "/api/"
site:example.com graphql
site:example.com "_next/static"
site:example.com swagger
site:example.com openapi
site:example.com filetype:json
site:example.com filetype:log
site:example.com admin
site:example.com staging
site:example.com dev
The mistake most people make is running dorks randomly. The better approach is to turn dorking into a small repeatable pipeline.

Setup Guide:

#!/usr/bin/env bash
set -euo pipefail

TARGET="${1:-}"
if [ -z "$TARGET" ]; then
    echo "Usage: ./yadexloop.sh example.com"
    exit 1
fi

TARGET_CLEAN=$(echo "$TARGET" | sed 's#https://##;s#http://##;s#/.*##')
OUTDIR="yandex-loop-output/$TARGET_CLEAN"
mkdir -p "$OUTDIR"

DORKS=(
    "site:$TARGET_CLEAN /api/"
    "site:$TARGET_CLEAN graphql"
    "site:$TARGET_CLEAN swagger"
    "site:$TARGET_CLEAN openapi"
    "site:$TARGET_CLEAN redoc"
    "site:$TARGET_CLEAN admin"
    "site:$TARGET_CLEAN login"
    "site:$TARGET_CLEAN dashboard"
    "site:$TARGET_CLEAN staging"
    "site:$TARGET_CLEAN dev"
    "site:$TARGET_CLEAN test"
    "site:$TARGET_CLEAN beta"
    "site:$TARGET_CLEAN internal"
    "site:$TARGET_CLEAN _next/static"
    "site:$TARGET_CLEAN _next/data"
    "site:$TARGET_CLEAN chunk.js"
    "site:$TARGET_CLEAN app.js"
    "site:$TARGET_CLEAN main.js"
    "site:$TARGET_CLEAN firebase"
    "site:$TARGET_CLEAN s3.amazonaws.com"
    "site:$TARGET_CLEAN config"
    "site:$TARGET_CLEAN token"
    "site:$TARGET_CLEAN secret"
    "site:$TARGET_CLEAN password"
    "site:$TARGET_CLEAN filetype:json"
    "site:$TARGET_CLEAN filetype:xml"
    "site:$TARGET_CLEAN filetype:txt"
    "site:$TARGET_CLEAN filetype:log"
    "site:$TARGET_CLEAN filetype:pdf"
)

echo "[*] Target: $TARGET_CLEAN"
echo "[*] Output: $OUTDIR"
echo "[*] Running Yandex dork loop..."

RAW="$OUTDIR/raw_urls.txt"
FINAL="$OUTDIR/urls.txt"
: > "$RAW"

for DORK in "${DORKS[@]}"; do
    echo "[→] $DORK"
    QUERY=$(python3 -c "import urllib.parse; print(urllib.parse.quote_plus('$DORK'))")
    curl -ksL \
        -A "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 Chrome Safari/537.36" \
        "https://yandex.com/search/?text=$QUERY" \
        | grep -Eo 'https?://[^"<> ]+' \
        | sed 's/&amp;/\&/g' \
        | grep -i "$TARGET_CLEAN" \
        >> "$RAW" || true
    sleep 5
done

cat "$RAW" \
    | sed 's/#.*//' \
    | sed 's/[),;]*$//' \
    | sort -u \
    > "$FINAL"

grep -Ei '/api/|graphql|swagger|openapi|redoc' "$FINAL" > "$OUTDIR/api.txt" || true
grep -Ei '_next|\.js|chunk|webpack|static' "$FINAL" > "$OUTDIR/javascript.txt" || true
grep -Ei 'admin|login|dashboard|signin|auth' "$FINAL" > "$OUTDIR/auth_admin.txt" || true
grep -Ei 'staging|dev|test|beta|internal' "$FINAL" > "$OUTDIR/environments.txt" || true
grep -Ei '\.json|\.xml|\.txt|\.log|\.env' "$FINAL" > "$OUTDIR/files.txt" || true
grep -Ei '\.pdf|\.doc|\.docx|\.xls|\.xlsx|\.csv' "$FINAL" > "$OUTDIR/documents.txt" || true

echo ""
echo "[✅] Done"
echo "[*] Total URLs: $(wc -l < "$FINAL")"
echo "[*] Saved to: $FINAL"
echo ""
echo "Buckets:"
echo "  API: $(wc -l < "$OUTDIR/api.txt" 2>/dev/null || echo 0)"
echo "  JavaScript: $(wc -l < "$OUTDIR/javascript.txt" 2>/dev/null || echo 0)"
echo "  Auth/Admin: $(wc -l < "$OUTDIR/auth_admin.txt" 2>/dev/null || echo 0)"
echo "  Environments: $(wc -l < "$OUTDIR/environments.txt" 2>/dev/null || echo 0)"
echo "  Files: $(wc -l < "$OUTDIR/files.txt" 2>/dev/null || echo 0)"
echo "  Documents: $(wc -l < "$OUTDIR/documents.txt" 2>/dev/null || echo 0)"
Save it:

vim yadexloop.sh
Then run:

chmod +x yadexloop.sh
Install basic requirements:

sudo apt update
sudo apt install curl grep sed coreutils python3 -y
Run it:

./yadexloop.sh example.com
Main result:

yandex-loop-output/example.com/urls.txt
Useful buckets:

api.txt
javascript.txt
auth_admin.txt
environments.txt
files.txt
documents.txt
Final Thought:

Yandex dorking is not about noise. It is about finding forgotten indexed signals that can help you understand the real attack surface. The hidden gems are usually not obvious.

---

That's the full article. It's a practical recon guide on using Yandex dorking systematically — moving beyond random one-off queries to a repeatable pipeline that discovers APIs, JS files, auth pages, staging environments, exposed config files, and documents from indexed URLs for a target domain.